import { Command, flags } from '@oclif/command';
import { Meta } from '../lib/policy/model';
export default class Show extends Command {
    static description: string;
    static flags: {
        help: import("@oclif/parser/lib/flags").IBooleanFlag<void>;
        force: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        policy: flags.IOptionFlag<string | undefined>;
        module: flags.IOptionFlag<string | undefined>;
        snapshot: flags.IOptionFlag<string | undefined>;
        check: flags.IOptionFlag<string | undefined>;
    };
    run(): Promise<void>;
    showSummary(result: {
        [key: string]: {
            [key: string]: Meta;
        };
    }): void;
    showPolicy(policyMeta: Meta): void;
}
