"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePolicyPath = exports.resolveModulePath = void 0;
const path = require("path");
const constant_1 = require("../constant");
const PWD_PATH = process.cwd();
function resolveModulePath(moduleName, moduleUses) {
    if (moduleName === constant_1.ROOT_MODULE_NAME) {
        return PWD_PATH;
    }
    const modulePath = moduleUses.startsWith('./') ? path.resolve(moduleUses) :
        path.join(PWD_PATH, constant_1.DEFAULT_DEEPLINT_FOLDER, constant_1.DEFAULT_DEEPLINT_MODULE_FOLDER, moduleName);
    return modulePath;
}
exports.resolveModulePath = resolveModulePath;
function resolvePolicyPath(moduleName, policyName, policyUses) {
    const policyPath = policyUses.startsWith('./') ? path.resolve(policyUses) :
        path.join(PWD_PATH, constant_1.DEFAULT_DEEPLINT_FOLDER, constant_1.DEFAULT_DEEPLINT_MODULE_FOLDER, moduleName, constant_1.DEFAULT_DEEPLINT_POLICY_FOLDER, policyName);
    return policyPath;
}
exports.resolvePolicyPath = resolvePolicyPath;
