import { Meta, Resource, Snapshot } from './model';
export declare class Context {
    readonly meta: Meta;
    readonly inputs: {
        [key: string]: any;
    };
    constructor(meta: Meta, inputs: {
        [key: string]: any;
    });
}
export declare class CheckContext extends Context {
    private readonly snapshot;
    constructor(meta: Meta, inputs: {
        [key: string]: any;
    }, snapshot: Snapshot);
    getResources(): {
        [key: string]: Resource[];
    };
}
