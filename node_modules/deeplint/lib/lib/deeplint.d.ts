import { CheckingResults, FixingResults, Meta, Snapshot } from './policy/model';
export declare class Deeplint {
    private readonly deepLintConfig;
    private readonly modules;
    private constructor();
    static build(configFile?: string): Promise<Deeplint>;
    getPoliciesMeta(): {
        [key: string]: {
            [key: string]: Meta;
        };
    };
    snap(): Promise<{
        [key: string]: {
            [key: string]: Snapshot;
        };
    }>;
    check(snapshots: {
        [key: string]: {
            [key: string]: Snapshot;
        };
    }): Promise<{
        [key: string]: {
            [key: string]: CheckingResults;
        };
    }>;
    fix(checkingResults: {
        [key: string]: {
            [key: string]: CheckingResults;
        };
    }): Promise<{
        [key: string]: {
            [key: string]: FixingResults;
        };
    }>;
}
