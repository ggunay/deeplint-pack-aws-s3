import { AwsProvider } from '../helper/aws-provider';
import { Resource } from '@deeplint/deepscanner-base';
export declare class EC2InstanceProvider extends AwsProvider {
    static readonly RESOURCE_TYPE = "aws::ec2::instance";
    collect(context: {
        [key: string]: any;
    }): Promise<Resource[]>;
    private listAllInstance;
}
