import { AwsProvider } from '../helper/aws-provider';
import { Resource } from '@deeplint/deepscanner-base';
export declare class VPCProvider extends AwsProvider {
    static readonly RESOURCE_TYPE = "aws::ec2::vpc";
    collect(context: {
        [key: string]: any;
    }): Promise<Resource[]>;
    private listAllVpc;
}
