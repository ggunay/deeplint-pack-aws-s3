import { AwsProvider } from '../helper/aws-provider';
import { Resource } from '@deeplint/deepscanner-base';
export declare class VPCSubnetProvider extends AwsProvider {
    static readonly RESOURCE_TYPE = "aws::ec2::subnet";
    collect(context: {
        [key: string]: any;
    }): Promise<Resource[]>;
    private listAllSubnet;
}
