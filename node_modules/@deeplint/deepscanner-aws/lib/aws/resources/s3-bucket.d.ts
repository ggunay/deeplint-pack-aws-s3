import { AwsProvider } from '../helper/aws-provider';
import { Resource } from '@deeplint/deepscanner-base';
export declare class S3BucketProvider extends AwsProvider {
    static readonly RESOURCE_TYPE = "aws::s3::bucket";
    collect(context: {
        [key: string]: any;
    }): Promise<Resource[]>;
    private listAllBuckets;
}
