import { AwsProvider } from '../helper/aws-provider';
import { Resource } from '@deeplint/deepscanner-base';
export declare class EBSVolumeProvider extends AwsProvider {
    static readonly RESOURCE_TYPE = "aws::ebs::volume";
    collect(context: {
        [key: string]: any;
    }): Promise<Resource[]>;
    private listAllVolume;
}
