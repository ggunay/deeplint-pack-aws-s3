import { AwsProvider } from '../helper/aws-provider';
import { Resource } from '@deeplint/deepscanner-base';
export declare class EC2EIPProvider extends AwsProvider {
    static readonly RESOURCE_TYPE = "aws::ec2::eip";
    collect(context: {
        [key: string]: any;
    }): Promise<Resource[]>;
    private listAllAddresses;
}
