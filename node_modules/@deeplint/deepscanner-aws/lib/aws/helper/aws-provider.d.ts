import * as AWS from 'aws-sdk';
import { BaseProvider } from '@deeplint/deepscanner-base';
export declare abstract class AwsProvider extends BaseProvider {
    private regions;
    getClient(serviceName: string, region: string): AWS.Service;
    setRegions(regions: string | string[]): void;
    getRegions(serviceName: string): string[];
}
